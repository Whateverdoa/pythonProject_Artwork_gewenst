# program will run asyncio it will look in places
# and it will move files around
# also it needs to clean  folders
from pathlib import Path

# I will make a test situation.
# It will work with switch but when ready maybe a standalone

from Paths.paden import *

p=pad_naar_item_nummer_folder_maker(806321063359)